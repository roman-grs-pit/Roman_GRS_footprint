name: static

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is intentionally disabled. GitHub Pages is deployed by docs.yml."
